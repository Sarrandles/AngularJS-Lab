(function() {

  angular
    .module("moduleApp", []);
    //first parameter is name of the module and the 2nd is an array (dependencies the module may need to use)
    //to use it elsewhere you use:
    // angular
    // .module("name")

    //ng-repeat will create a new element for each thing we target and want to repeat
    //filters help you sort through content.

})();
